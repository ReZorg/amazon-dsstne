# DSSTNE Templates

This directory contains reusable template config files grouped by DSSTNE definition language:

- `CDL/`: Command Definition Language categories
- `LDL/`: Layer Definition Language categories
- `DDL/`: Data Definition Language categories

Each file targets one distinct category and provides a normalized structure to improve consistency and interop.
